// NOTE: This interface is specific to HistoryStateManager and should probably
//       be moved or renamed.  Keeping it temporarily to satisfy the "continue" instruction.
export interface IHistoryState {
    // Define the structure of your history state
    // This could be a snapshot of the entire game state or a more specific
    // representation of a state change.
    // Example:
    // gameState: GameState;
    // timestamp: Date;
}
